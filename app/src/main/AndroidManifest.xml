<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teknikelektrounpad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TeknikElektroUnpad">
        <activity android:name=".labKomputer_Act"></activity>
        <activity android:name=".fasilitas_activity" />
        <activity android:name=".ganjil_Act" />
        <activity android:name=".jadwal_detailAct" />
        <activity android:name=".kurikulum_detailAct" />
        <activity android:name=".alumni_detailAct" />
        <activity android:name=".akademik_activity" />
        <activity android:name=".activity_home" />
        <activity android:name=".GetStartedAct" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>